<%-include('./includes/head.ejs') %> 
    <h1>Users</h1>
    <% if(users.length>0){ %>
         <ul>
            <% for(let user of users) {%> 
            <li><%=user.name %> </li>
            <% } %> 
        </ul>
        <% }else{ %>
            <h1>No user found</h1> 
            <% } %> 
   
    <%-include('./includes/foot.ejs') %> 